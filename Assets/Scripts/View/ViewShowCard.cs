using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ViewShowCard : MonoBehaviour
{
    // Start is called before the first frame update
    void OnMouseUpAsButton()
    {
        gameObject.SetActive(false);
    }
}
